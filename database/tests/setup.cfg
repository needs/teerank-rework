[coverage:run]
omit = protos/*

[tool:pytest]
addopts =
	-p no:warnings
	--flake8
	--cov
	--cov-report term-missing
	--mypy
	--black
	--isort
testpaths =
	sources
	tests
filterwarnings =
	ignore::DeprecationWarning

[mypy]
mypy_path = ./sources:./protos:./tests

[tool.isort]
profile = "black"
