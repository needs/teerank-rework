[coverage:run]
omit = protos/*

[tool:pytest]
addopts =
	--pylint
	--cov
	--cov-report term-missing
	--mypy
testpaths =
	sources
	tests
filterwarnings =
	ignore::DeprecationWarning

[pylint]
load-plugins=pylint_protobuf
